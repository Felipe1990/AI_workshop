# Para instalar los paquetes que vamos autilizar en el taller ejecute la
# siguiente línea de código
#
# Para ejecutarla seleccione el texto y oprima Ctrl + Enter o también le puede dar
# click al botón run en la barra de botones (el ícono es una flecha verde
# indicando a la derecha sobre un cuadro blanco)

install.packages(c("dplyr", "tidyr", 
                   "ggplot2" ,"esquisse",
                   "lubridate"))
